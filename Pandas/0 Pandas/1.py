
# Pandas --
''' Python library used for working with structured data sets (tables). It has functions for analyzing, cleaning,
     exploring, and manipulating data. Similar to data tables in SQL and Excel. '''

# Dataframe -- 
''' It's a structured data constructed with rows and columns, similar to a SQL tables or Excel tables. '''


# check installed pandas version
import pandas as pd
print(pd.__version__)

